# Runtime state — never commit
state/
repos/

# Local secrets
.env
.env.*
*.private-key.pem
*.pem

# Per-user config (config.yaml.example is the committed template)
config.yaml

# Editor / OS junk
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code per-project local-only settings
.claude/settings.local.json
