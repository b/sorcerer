# Per-project sorcerer workspace (state, config, bare clones, logs)
.sorcerer/

# Legacy layout (pre-slice-15) — still gitignored so migration is safe
state/
repos/
config.json
config.yaml

# Local secrets
.env
.env.*
*.private-key.pem
*.pem

# Editor / OS junk
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code per-project local-only settings
.claude/settings.local.json
